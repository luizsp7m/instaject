export type User = {
  name: string;
  email: string;
  image: string;
}

export type Technology = {
  id: string;
  email: string;
  name: string;
  imageUrl: string;
  created_at: string;
}

export type ImageLocal = {
  image: string;
  name: string;
  size: string;
}